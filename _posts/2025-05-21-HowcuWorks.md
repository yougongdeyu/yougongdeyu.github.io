## cpu是如何工作的
### cpu有哪些模块
![cpu结构](/image/cpu/CPUStructure.png)

控制器
运算器
cache （数据cache和指令cache）
### 这些模块是怎么工作的
控制器把变量写入运算器的寄存器
控制器根据指令控制alu的计算逻辑
运算器自己会计算

cache会从内存加载，如果遇到缓存未命中的情况，会主动从内存加载一批指令。
若访问0x1000处的指令未命中，则加载0x1000~0x103F（64字节）到I-Cache。
同时cache销毁的时候也会修改内存里面对应的数据.通过数据总线，指令总线寻址读取数据。
#### 这里cache和内存缺页异常有什么不一样？ todo放到后面
这个是硬件缓存，那个是虚拟地址（磁盘）为映射到物理内存，触发缺页异常，操作系统内核会去处理。
都是缓存，未命中都会加载一部分。

### cpu有多少种周期
取指周期
执行周期

然后有好几种指令周期要了解下，主要是中断，中断时候cpu的上下文切换，以及操作系统上下文切换。
## 中断是什么
cpu像是一个while循环，然后中断是一个cpu开放的口子接受外部信息。
有硬件中断
有软件中断，比如syscall。
传递数据的方式是内存和寄存器，比如单片机就是寄存器暴露出来的。
### 中断是如何工作的
cpu有一个指针寄存器（SP）,指向一个内存区域，然后中断电路的逻辑是把cpu的上下文以堆栈的方式保存在内存里面。
sp就是个指向一个内存区域的指针，然后访问方式是堆栈的方式。然后要保存的内容也都固定，所以就可以恢复和执行或者入栈。

### 中断在哪个周期里面执行
每个指令周期都会检测，比如取指周期检测一次，但在指令执行完之后会检查中断并判断要不要立即执行。
现代cpu优化，可能略有改动.

## 代码在CPU里面是如何工作的
c = a + b 这行代码是如何工作的

## 指令和内存是如何关联起来的
同时cache销毁的时候也会修改内存里面对应的数据。

## 数据cache和指令cache

## 中断


## 上下文切换

## yield await什么的


## 中断嵌套可以多少次

每次中断嵌套，CPU 都会：
自动保存当前中断上下文（寄存器、PC 等）到栈上；
如果中断继续嵌套，会不断压栈。
⚠️ 所以中断嵌套的次数真正受限于栈的深度（空间）。

示例：
假设每次中断压栈占用 32 字节，而堆栈大小为 1KB：
最多嵌套：1024 ÷ 32 = 32 层
而很多嵌套指令执行也可能再用栈，所以安全嵌套层数要打折扣

## 这里的自cpu自动自动保存当前中断上下文，这里自动保存到哪里？保存到到cpu内部，保存到内核管理的某个内存区域还是？
保存在内存里面

## 既然保存到内存里面，那么这个内存区域的管理是谁管理的，cpu自己管理的？还是操作系统管理的
中断发生时的上下文保存，是由 CPU 的硬件机制自动执行的，但保存的位置（栈区）是由当前运行任务的**栈指针（SP）指向的内存地址所决定的。
🧠 而这个「栈指针指向的内存区域」，其分配与管理权归属是操作系统（OS）或裸机开发者自己负责的，不是 CPU 来分配内存
其实就是sp指向一段内存区域，然后这个区域的访问方式按照堆栈的方式操作。

无非操作系统的话它会有个虚拟内存，对应的物理内存就可能不是像裸机那样固定的从某个内存地址开始了。

## cpu中断会自动保存上下文，

 ## 保存哪些内容
除了cache不保存，其他都保存，比如寄存器，计数器。


## 中断发生在哪个指令周期
中断发生在 取指，译码，执行...中的某两个周期之间，

## 响应中断是什么时候
在指令执行完指令之后相应


## 调度器如何进行线程切换的  我想控制cpu从某个指令开始执行，cpu提供的指令是什么呢？？
轮到内核线程执行的时候（时间片或者中断），调度器把cpu的上下文从内存准备好，cpu提供了指令从内存读取数据，设置寄存器这些东西
然后设置程序计数器pc，然后就执行了。
## 调度器如何备份线程的
把cpu上下文存到内存里面，同时也是用的cpu指令，模拟类似cpu中断做的入栈操作，只不过cpu入栈是cpu自己的硬件行为。

## 调度器的调度工作是如何执行的，依附于某个cpu核心，专门调度工作么？
每个cpu核心都有一个内核调度器，cpu通过中断或者定时器来调度

## 切换cpu上下文，和操作系统上下文  
这个上下文的差别



## 并发问题发生在指令周期还是指令指令之间

发生在指令指令之间，粒度没有cpu中断那么细粒度。只不过a=a+1 这个是分成三个cpu指令，所以一个线程刚设置了寄存器，另一个可能计算后写回内存了
如果这三个指令不能被打断，那不就可以高层抽线成锁了。
## 那其实调度器的功能并不复杂，耗时也很小，也保证了用户线程的高可用


# cpu提供哪些原子指令 使得操作系统可以高层封装成锁
XCHG reg, mem
交换寄存器和内存的值，原子执行。x86的XCHG自带隐式LOCK，所以本身就是原子操作。

LOCK CMPXCHG mem, reg
比较内存中的值和寄存器中的值，如果相等，则将寄存器的值写入内存，否则将内存的值写回寄存器。常用来实现CAS。

LOCK ADD mem, reg
对内存加法操作，原子执行。

## jvm如何映射线程的fork clone？？？？？

## 一个rest api 所用到的资源有哪些

## 线程映射到操作系统之后如何拿到结果

## 线程 clone  操作了哪些东西，从cpu执行的角度，从jvm 的角度
其实是设置一个cpu的上下文，比如pc，并且还有sp，就是指定栈指针，这样方法调用了这些才可以实现。






## 就是说编译器通过指令保证了局部变量在sp附近分配，而不是说cpu默认定义变量的时候在sp附近定义
完全正确

## 那cpu的sp的意义何在，跟普通寄存器有啥区别，
SP（Stack Pointer，栈指针）是一个专用寄存器，用来标记当前栈的顶部地址。
它的特别之处在于：

编译器和操作系统约定俗成地使用它来进行函数调用、返回、局部变量分配、中断保存等行为的“锚点”。

等于说方法调用么模拟了中断嵌套的cpu内部硬件电路固化的逻辑

并且原则上说想怎么写就怎么写，没有内存模型也可以用，只是有这个模型了之后，可以更好的解决问题。
比如堆栈  提供了 抽象出了方法的含义 的可能，也跟那个cpu本来的堆栈保持设计上的一致。

其实cpu做的事情很简单，越简单的事情越灵活，充满无限可能，cpu就是几个周期，不断读取指令 ，我管你指令在内存在哪里，  执行就行了。（但是太灵活了不知道怎么用，那就抽象下堆栈，这样可以抽象出方法调用，有的语言函数是头等公民，可见函数的重要性，同时某种程度上也是一种限制，但是任何事情都不能两全的，不够灵活之后肯定有做不到的，但肯定有更容易做到的，人生也就是这样，选了这个，有这个的好处，选另一个有。。。）
内存也是这样，只有01，所以才无限可能，可以创造那么多数据结构。


### 那为啥不能像中断那样，自己保存cpu上下文，而是要编译器自己维护变量这些东西


##  我的意思是说就不让编译器自己做了，直接让cpu通过类似中断嵌套的功能实现栈的保存不就好了
中断开销大，编译器是种优化，就好像调度器调度以指令为单位，而中断以指令周期为检测单位一样（检测，但是执行还是等指令结束），更加的细粒度，都是优化。

## 既然中断能保存上下文，那方法切换肯定也可以
### 你的思路很有洞察力，确实中断保存上下文的机制是自动的、硬件支持的，那为什么方法调用（函数调用）不直接用类似中断的自动上下文保存呢？我们可以从设计目标和性能效率等方面来分析：

### 为什么函数调用不直接像中断那样自动保存上下文？
1. 中断上下文保存是“重开关”，开销大，函数调用频率太高，自动保存开销难接受
中断是“异步、非预期”的，需要尽可能完整地保存所有上下文
现代 CPU 把函数调用的上下文管理职责交给编译器，由编译器按照调用约定生成代码，显式入栈/出栈保存需要的信息。

## 那操作系统切换线程呢，为啥不能重用中断。
方法 < 中断 < 线程切换   要处理的cpu上下文数据量
![WhyCannotReuseInter](/image/cpu/WhyCannotReuseInter.png)

## 操作系统维护的线程的 cpu上下文存在哪里？是不是对线程的定义的地方保存着，操作系统取维护

## 开销有多大，切换线程和切换进程比较


## 其实最重要的是这个点， 软硬结合，模拟了一个cpu, 同时管理了内存,jvm就做了这件事。

现代 CPU 指令集和编译器协同优化
编译器根据调用约定生成最优代码，只保存必须保存的寄存器；

CPU提供指令支持（如 CALL、RET）简化函数调用，但不自动保存所有上下文；

这样兼顾灵活性和效率

## 还有最后一个点，就是操作系统 mmu ，内存这个 跟cpu关系。这个弄通了也就ok了，还有就是  本地线程栈（Native Thread Stack
## 那如何保证这些jvm核心线程优先于 业务上的jvm线程执行，还是说有优先级，还是说都可以
操作系统给的有口子，保证线程优先级高一点。

## cpu给了哪些口子供编程

一个是内存的管理，模拟中断那样管理cpu上下文管理方法栈。

一个是操作系统提供的本地线程栈，里面可以穿插java的代码，然后cpu执行的时候一会执行c++ 代码，一会执行java代码
但是从jvm的角度将，jvm方法栈里面只有java代码，因为只是操作系统本地方法栈的子集

其实内存控制就是编排么，然后还可以控制cpu去哪执行，就无限的创造力了。


## 事件编程 以及还有什么编程

## 代码执行的时候是全部加载到内存还是逐步加载。？ 还有classloader
## pthread_create做了什么 就是告诉操作系统：“我需要一个新线程，请给我一块独立栈空间，设置好起点和栈顶，然后你调度它执行”
分配一块新的线程栈内存（通常几百 KB 到几 MB，可配置）；

用来存放该线程调用过程中的栈帧（局部变量、返回地址、保存的寄存器等）；

栈顶地址会设置到 SP（Stack Pointer）寄存器。

简单的很

 ## SP就跟cpu裸跑的时候是什么样，是跟中断处理那个cpu上线文保存的栈顶指针一个东西么？
 单品机里面的方法调用又栈这个东西么？


## 那执行到访问jvm堆的代码cpu什么行为
对cpu来说都一样，和普通局部变量没区别。都是内存，无非线程堆栈里面的是 操作系统本地方法栈里面的内存而已。







## c语言编译的代码是什么样的，从cpu执行的角度分析下，比如比如cpu 的指令cache，什么时候从磁盘进入内存，什么时候从内存进入cache


操作系统通过 execve() 把 ELF 文件从 磁盘读取到内存, 然后就可以执行了。


## 如果程序很大，比如10m，也是一次加入到内存么？
不是的， 每个进程都有自己的虚拟地址，靠的页错误机制，都有一个2g的内存， 映射了，但是如果访问的时候没有加载到内存，mmu会自己加载的，
这个对开发人员是透明的，时间也很短暂，
## 页错误机制 性能有多快
几百纳秒到几微秒，严重的页错误ssd几十微秒到几百微秒，机械的话几毫秒甚至更长，并且又内存锁定和预热机制。比如db了，jvm都会这么干。

## jvm full jc 的性能？
几十到几百毫秒，所以操作系统的性能是最快的，运行在之上的程序都稍差一点，像jvm。



## 那比如一个library，我只用到里面的一个方法，那cpu执行的时候是 把整个library代码加载进来么？
假设你引用了 libtensorflow.so，这个库有 200 MB，你只用其中一个 inference() 方法：
程序启动时：这个库在虚拟地址空间里被映射了，但并未完全加载。
你调用 inference()：
系统只把包含 inference() 的几页（比如 4KB x 3 页）从磁盘读入内存。
剩下 99% 的代码根本不会加载，更不会占用 CPU cache。


## 


必要的 .text（指令段）、.data（初始化数据段）

对用啥
 execve() 跟syscall是一个级别的东西么？

## 指令段数据段是什么意思，举例说明，对应到cpu是数据cache和指令cache么？


 ## 就是说操作系统抽象了一个内存模型，比如堆栈，堆，数据段，然后编译器也follow这个规则，把数据组织好，然后操作系统消费这些数据

配合

 这边很有意思，就是进程的内存模型，栈，堆，未初始化，初始化数据，代码段。
然后编译器编译出的文件也是按照这个规则来生成对应的文件的。


 然后我觉得jvm的模型也是 在这个里面受控的。就是说jvm在操作系统的内存模型里面开创出自己的天地。


 ## jvm 如何用多线程的，等于是在一个进程里面用了多个cpu核心，操作系统如何支持这种行为的。

## class在jvm里面被消费的流程
![howClassWorksInJVM](/image/cpu/howClassWorksInJVM.png)
### jvm解释执行字节码，那解释后是放在进程模型的.text段？
.text 段是 操作系统加载 ELF 或 PE 等二进制可执行文件时，包含静态机器码的内存段。而解释执行不会生成静态机器码，也就不会写入 .text 段。
其实既然执行的是.text段代码，那.text段可以自己再包装一层啊，包装一层去消费字节码。就跟写代码一样一样的。所以这里的解释器其实就是C/C++ 编写的解释器循环读取并执行。
![JVMInterpreter](/image/cpu/JVMInterpreter.png)
![JVMInterpreter](/image/cpu/JVMINterpreter2.png)
### 就是说jvm本身就是.text段的程序
![JVMInterpreter](/image/cpu/WhereIsClass.png)

解释器本身是 JVM 的一部分，是编译后放在 .text 段中的可执行代码。
字节码保存在：不是 .text，而是 .data 或匿名映射（anonymous mmap 区域）。

## 那类加载器的作用是什么，在哪部分，什么时候运行
![ClassLoader](/image/cpu/ClassLoader.png)

 ## 那其实就是说class也不都是一次全部加入内存，而是跟操作系统执行代码一样，也不是全部加载，而是缺页的时候再从磁盘加载到内存，同样道理，class模拟了这种行为
![DesignPatternOfCLassloader](/image/cpu/DesignPatternOfCLassloader.png)
 你看又是同样的思想，其实就是嵌套的思想。阴生阳，阳生阴。阴阳其实也是一种配合。

## 类加载器性能怎么样？
加载一个常规 .class 文件（几十 KB）：几十微秒；
复杂的反射 + 自定义加载器 + 网络加载：几十到几百毫秒不等；
Spring Boot 启动时常会加载数千个类，但这些只在启动阶段加载一次，运行时几乎不会再次触发类加载器。

 ## execve() 跟syscall是一个级别的东西么？
 不是的，execve()是用户态的 C 库函数接口封装的内核通过syscall提供的功能，底层还是syscall
execve功能是把c语言编译的东西放到操作系统的内存模型里面，然后执行。
 ### c语言编译的代码是什么样的，从cpu执行的角度分析下，比如比如cpu 的指令cache，什么时候从磁盘进入内存，什么时候从内存进入cache
C 源码 --(编译)--> 机器码(ELF等格式) --(操作系统加载)--> 内存 --(CPU预取/Cache命中)--> 指令Cache --(CPU执行)
编译生成 ELF文件，不同的架构，指令集，内存大小，都不一样。
![ELF和cpu](/image/cpu/CCodeAndCpu.png)
![ELF和cpu](/image/cpu/CCodeAndCacheAndDisk.png)
#### ELF文件结构以及跟cpu和操作系统关系？

![ELF和cpu](/image/cpu/ELF.png)
![ELF和cpu](/image/cpu/ELF2.png)
![ELF和cpu](/image/cpu/ELF3.png)
![ELF和cpu](/image/cpu/ELF4.png)
![ELF 和操作系统](/image/cpu/ELF5.png)
![ELF 和操作系统](/image/cpu/ELF6.png)
其实 就是说操作系统抽象了一个内存模型，比如堆栈，堆，数据段，然后编译器也follow这个规则，把数据组织好，然后操作系统安排cpu消费这些数据。
或者说编译器组织了操作系统要的格式，操作系统加载并维护这些格式，CPU 执行这些段中的代码和数据。
##### 链接器是啥？
把多个目标文件（.o 或 .obj）和库文件，合并成一个完整的可执行程序或共享库。


### 如果程序很大，比如10m，也是一次加入到内存么？
不是的，启动的时候映射好，操作系统比如execve执行代码前加载到内存的也是一部分而已，然后后续cpu缓存会硬件访存，物理内存里面没有也会自己访问磁盘

使程序本身很大，比如 10MB 或更多，操作系统在加载时也不是一次性把它全部读入内存，而是采用了按需加载（lazy loading）+ 分页机制来高效管理内存使用。
![page fault Exception](/image/cpu/pageException.png)
#### 页错误机制 性能有多快
虚拟内存映射
![VirtualMemoryMap](/image/cpu/VirtualMemoryMap.png)
所访问的虚拟页已经映射在某个内存中，比如共享内存或文件映射中: 几百纳秒 ~ 几微秒。
严重页错误：几十微秒到几百微秒

### 既然看了操作系统的性能，那看下jvm的，jvm full gc一般几毫秒
假设你的 Spring Boot 服务堆大小为 4 GB，使用 G1 GC：
在无大量对象晋升的情况下，Full GC 可能只需 几十毫秒
如果老年代碎片严重、对象回收不充分，可能要 200ms~800ms





 ## 不能直接全部把类加载进内存么？这样性能更好。
 类加载都是微秒级别完成的，双亲委派也是优化过的，减少了类的查找范围，提高了命中率。所以很值得。
 有时候找女朋友不要太苛刻，人家单眼皮，但是是个富婆，那就算了，没有双眼皮换那么多钱，性价比很高的，所以没必要全部加进内存，类加载器性价比很高。

 ## syscall传递参数是寄存器和指向用户态内存的指针。比如用户态是个结构体了那种大的对象。通过这种方式实现数据传递。

 

 ## 但是这种大的数据结构是如何实现和消费的，如何定义的 。

 其实也是配合。

 就是有人定义规则，有人按照规则去设置内存，然后有人按照规则去消费内存。

 所谓的数据结构就是 某个规则的数据，加上对应的访问逻辑而已。

 java的chart， int，long，也都不过如此而已。


 ## 一个进程的内存不是只能被这个进程共享么？一个进程一个cpu， 那java里面其实是多线程，那多个线程大概率是多个cpu核心，如何消费呢。

 一个进程可以有多个cpu来运行，或者说阉割版本的进程。进程是资源分配的单元，而线程在这个基础上更细分了下，按cpu来划分了下。

 然后cpu内存共享的部分，并且多个核心有缓存一致保证各个核心的缓存是一致的（为啥是缓存，因为缓存的一致了，缓存对应的内存也一致，这之前问过缓存的事情，退出缓存的时候会写会内存，所以缓存一致数据最终就是一致了）



 ## 现代操作系统 通过编译器，约定商量共同组成了

 ## 如果是单片机，那怎么执行，没有mmu，代码加载到内存一次性的么？

 不是的，单片机直接从flash读取和执行指令，然后内存也是固定的，所以
 就是说编译器保证了内存模型，这也是为啥要根据不同cpu做移植的原因？我是说单片机的程序在不同cpu上运行的时候需要用不同编译器编译才可以，因为cpu太灵活了，但是资源比如内存又是受限的，只能编译器保证下，才好用。
 然后推广下，指令集可能也不一样，虽然做的事情可能一样，（在 AVR 上，访问 Flash 要用 LPM 指令，而 RAM 用 LD/ST。）所以这也是编译器的脏活累活。说到底还是配合,cpu定义了指令，编译器follow。


## java

 ## java用了多少条指令，x86有多少条指令。
java 有200多一点点指令

## 还有之前哪个jvm线程模型映射到操作系统内存模型，应该之前理解不太对。不应该是这样，jvm是解释执行的，代码不在.text里面，哦不对，那是方法栈。方法栈是映射了的。

