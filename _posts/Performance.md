## 性能测试的基本套路是啥？


保持一个变量变，比如内存

## 四要素
并发数，吞吐量，相应时间，成功率/错误率

并发数，同一时刻并发处理多少请求，
吞吐量，一个单位时间比如一秒处理多少个请求，
相应时间，在多少时间内返回结果，比如p95=3s, 95%的请求3秒返回结果。
成功率/错误率 400/500 这种情况
![safsdf.png](/image/performance/performance.png) 

## cpu
70-20
## 内存泄露的曲线和正常曲线

cpu 正常曲线是上去下来，如果扩大内存吞吐量没上去，
内存泄露是增长的大于回收的。不断增长
##  往往最后才排查到io的情况。
一般io都是够的。
## soucegen
l10n，系统很慢。cpu大量的
内存和cpu占用很大，然后找到sql，然后找到业务，然后解决问题。

## ai的测试 ，主要是价格，所以没有换别的显卡。


## 怎么排查问题，也是改变一个变量然后性能有没有变好，如果变好，那就？？


## jemiter 和 loadrunner 


## grafana

## promithus

## jemiter 模拟用户操作，并且操作有时间间隔的。


## io很大，但是cpu负载不高，为啥
io密集型的业务

## ais的东西。


## nltk的库，模拟

## 测什么，

有没有数据返回
响应时间
关键字匹配。

## 难点，准备数据

## 多语言怎么测
法语网站，6000篇文章，测试下能不能搜索到文章就好。

## sample data 做测试。保证日常的cicd.

性能测试就是 测试并发数，吞吐量，相应时间，成功率/错误率, 然后方式就是改变一个变量，然后测试或者发现问题。
比如 我发现把cpu mem给大了，然后吞吐量并没有上去，那可能是代码没处理好。实际遇到的情况是 redis出问题了，导致db的内存 cpu特别高，后来找到对用的sql，找到对应的业务，然后发现问题，并解决的。

ai怎么测的。

测试就有没有返回值，相应时间。
ragas 结果

然后主要难在准别数据，一开始只英语，大数据量，后来multip language ,就测了个法语，准备了6k文章，然后在milvus是2w数据，然后再后来就 cicd里面的。


cscp 匿名化去标签化，

