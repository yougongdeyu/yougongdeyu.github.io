

os 内核 mmu 页表 tlb 缓存
进程切换，线程切换

操作系统信号量，

## java concurrent

## 锁是啥？
## os同步原语
## javaconcurrent 是啥



get set如果是互斥的，那即使不是线程安全的，那也没有线程安全的问题

## volatile 重排序 + 可见性

DCL 双重检查锁
![safsdf.png](/image/Spring/DCL.png)
1.指令重排导致未初始化使用导致出错（创建完成之后再设置变量）2.保证单例，3.重复创建或被覆盖

第一个if 保证单例
vatalile保证问题1
syhchronized + if 保证只创建一次，因为第一次if可能失效。
synchronized仅切只使用了一次 

## 因为volatile的可见性只有禁止重排序保证创建完后设置变量后拿到的是正常的变量。

那别的就没有问题？
## spring 单例模式为类似DCL
但为啥没有用volatile？因为没必要

synchronized 保证创建过程原子化，

所以是volatile的方式并不是synchronized区域的竞争问题，而是synchronized 之外的if可能拿到半成品的东西，或者说set是原子的，但是get可能跟set并发执行
## java aqs



## 三级缓存
1.aop，2.依赖和构建，3.单例。三级解决三个问题。
第三级缓存放factory（aop）
第二级放半成品的（依赖和构建）
第一层放单例的concurrentmap（单例）
一二缓存都是放线程不安全的hashmap，但get set如果是互斥的，那即使不是线程安全的，那也没有线程安全的问题。
A->B->A

被引用的时候放入第二级缓存。
即使单例模式里面对象构建一半，也是可以被使用的，将来会被设置好。
（votalite 之所以有问题，就是因为votalite没有被完整创建，但也被引用了。）
然后A好了会让自己只存在于一级缓存中。

过程 
![safsdf.png](/image/Spring/ThreeLevelCache.png)
## 单例保证线程安全？

安全
## 缓存 concurrnet 的lock如何工作的



## 红锁

## synchronized原理



工厂
三级缓存里面是工厂的工厂



## 线程池的原理

创建一个线程怎么创建的
集成thread类，然后实现run方法
执行的时候thread.start() 把方法放到操作系统调度队列，然后等到线程执行的时候从run执行

那线程池原理就是包装一层，run方法可以包装成while循环，从共享队列里面调用就可以了


## 线程的状态有哪些？

new 
runable 就绪和运行
block
waiting
timewaiting
terminated

## java栈在哪里

## 既然线程了，为啥微线程


## docker技术原理是啥


## 
普通用户态函数调用只保存必要的寄存器（caller-saved/ callee-saved），通过栈管理返回地址和局部变量；不像系统调用或线程切换需要保存整个 CPU 上下文。
aller-saved/ callee-saved
