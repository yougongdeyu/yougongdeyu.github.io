## 
什么是锁。


##     所以volatile是变量级别的原子性，和可见性，但是对于相关联的一系列操作的情况，比如一个方法里面的代码级别的，写和读不是互斥的。读是一直都能读到的，所以没有互斥性，就不是锁。
![](/image/java/volatile.png)

## synchronized 的流程
![](/image/java/synchronized.png)
![](/image/java/ObjectHeadAndMarkWord.png)
![](/image/java/ObjectHeadAndMarkWord2.png)

没有竞争，锁就没效。
可以是变量，也可以是一堆变量，加if else逻辑

跟那个三级缓存很象，都是协调着来，感觉有两个人商量着做事情一样。这个锁升级也是。

那是怎么区别是内粗n
## retranck lock的场景


## futex只是一个挂起，启动线程的os命令而已。进程的锁和线程队列，还是jvm自己管理。

## AQS


## 线程

## 线程池


## os提供服务的方式，或者说syscall，软中断
软中断，有好几个寄存器，就跟调用方法一样，如果寄存器不够用


## 道

分布式的道理，或者说计算的道，一个是代码管理计算，一个是消费数据的分布。就比如kafka为啥要zookeeper，因为代码是在所有集群都有的，并且不会变的，但是meta信息在哪，是不知道的。这个数据要有地方统一存储，那就要zk来保证。

还有就是aop，上面的zk也是aop，加一道逻辑或者说包一层，包好多层就行了。写代码逻辑这样，架构也是这样。

不要小看crud， 操作系统内部也都是一些crud，


锁的方面，其实也是语言层面的优化而已，os提供了操作原语，一开始高级语言使用os原语，但是后续就自己优化，然后自己造轮子了。


还有就是协商好，两边协商好，c语言内存模型是协商好的，网卡驱动是协商好的，os的原语是协商好的。甚至软中断的传递参数都是协商好的，还有springboot的yaml也是协商好的。
