比如、向量化处理、向量检索，LLM的调用和生成，最后服务的封装和系统部署



文章切分：
使用 NLTK 分句，保证每条句子为最小语义单元；并对超长句子进行拆分，防止token超限，对小句子进行合并，降低噪音，提高后续embeding质量，解决token长度限制和语义完整性问题。
句子向量化：
通过Embedding 模型对句子进行向量化，方便后续聚合操作，同时实现批量处理，优化大文档处理性能，提高系统吞吐量。
语义聚合：
基于句子的余弦相似度，将语义相近且相邻的句子聚合为一个chunk，保证语义连续性。同时对于超过token长度限制的超长cluster，进行拆分操作，确保每个Chunk在token长度限制的范围内。
提高了语义匹配的准确性，优化了检索结果的相关性，并有效降低了上下文断裂的风险。
服务封装：
基于gRPC的方式暴露服务，用Python实现文档解析、语义聚合、Chunking，Embedding，把Python处理文档解析的能力和Java业务逻辑处理很好的拆分开，
在保证系统高吞吐与低延迟的前提下，实现服务职责清晰拆分。
服务部署：
基于Docker构建标准化镜像，实现与同Pod内Java应用的高效集成，并适配公司CI流程；同时在Kubernetes上部署多节点集群，结合负载均衡与高可用策略，实现服务的弹性伸缩与稳定运行。
展望：
目前系统在处理大文件或高并发请求时存在性能瓶颈，未来计划通过引入 Kafka 与 Redis 构建异步任务处理机制优化架构
保证系统高可用性和响应稳定性，为后续高并发调用和大规模文档处理提供支撑。




gRPC + ProtoBuf 关系是啥

解决token长度限制 这个是谁


基于余弦相似度，对语义相近的句子按相邻顺序聚合，并对超长cluster拆分，保证每个Chunk在token长度限制内，提升语义匹配准确率
